# Legacy /tr/* Redirects zu Root
/tr/* /:splat 301!
/tr / 301!

# Legacy sitemap redirect
/functions/v1/sitemap /sitemap.xml 301!

# Trailing slash redirect (301) - P0 Fix
/:path*/ /:path* 301!

# SPA Fallback (must be last)
/* /index.html 200
